# PM temporary assets

PM or the Agent may place temporary mock-only media here when the Shared Design Library
does not yet contain a suitable resource. Use `image/`, `video/` or `other/` subfolders.

These files may be used for PM review and design-working prototypes. A design-final
revision must not reference them; Designer supplies the final resource through the
global Shared Design Library instead.
