# __FEATURE_TITLE__ — Wireframe review

`/prototype-wireframe __FEATURE_SLUG__` writes one static, greyscale HTML page per
configured viewport here (`desktop.html`, `compact-desktop.html`, `tablet.html`). They
show the at-rest composition — every zone and at-rest component role from
`surface-intent.yaml` as a labelled box — so the PM can settle band order and density
before any React is generated.

This folder is review material: excluded from the generation input hash, never imported
by the prototype, never part of the public build.

## Decisions taken on the wireframe

Record each one here in the PM's words, with the source file it belongs in. They are
written into `surface-intent.yaml` and `decisions.md` by `/prototype-revise` or
`/prototype-intake`.

| Date | Decision | Belongs in |
|---|---|---|
