# Temporary PM assets

Files here can be referenced as `mock:<file>` in content.json. They may reach a Strapi
draft but must be replaced by Design Library or existing Strapi media before
`page-pm-review`.
