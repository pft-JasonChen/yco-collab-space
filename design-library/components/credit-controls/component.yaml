schemaVersion: 1
id: credit-controls
displayName: Credit Controls and Generate Action Bar
classification: composite
status: pilot-approved
rd:
  sourcePackage: youcam-enhance-frontend
  sourceVersion: 1.34.1
  snapshot: yce-frontend-gm-260909
  portability: drop-in
  sourcePaths:
    - src/components/result-page/common/apply-button-with-coin-credit/index.js
    - src/components/result-page/common/apply-button-with-coin-credit/index.module.scss
    - src/components/result-page/common/apply-button-with-coin-credit/components/credit-badge/index.js
    - src/components/result-page/common/apply-button-with-coin-credit/components/credit-badge/index.module.scss
    - src/components/common/headers/components/credits-left/index.js
    - src/components/common/headers/components/credits-left/index.module.scss
    - public/assets/images/header/icon_credit.svg
    - public/assets/images/header/icon_add.svg
    - public/assets/images/loading.png
    - src/components/result-page/video-object-remover/index.module.scss
  sourceHashes:
    - { path: src/components/result-page/common/apply-button-with-coin-credit/index.js, sha256: 611713ab5342a987aab96d57f93d2898137b5aae76cb7597a73ec4c35208cb40 }
    - { path: src/components/result-page/common/apply-button-with-coin-credit/index.module.scss, sha256: fad4272e4cf334e1c04d1ec3a4a3b4c937096342ba1cdc35423e2196cea62fa1 }
    - { path: src/components/result-page/common/apply-button-with-coin-credit/components/credit-badge/index.js, sha256: 522a03001c2eeb67cefeef99d1b480d7bbfac934701db22ebe0c7dcf2e0cad78 }
    - { path: src/components/result-page/common/apply-button-with-coin-credit/components/credit-badge/index.module.scss, sha256: 82a20ff8ca70f9081d5c607c718611bc5451e59f77cdb9dde6cbd8b4ba787984 }
    - { path: src/components/common/headers/components/credits-left/index.js, sha256: e4cea9a8210fb78271c2cd03569803f2592fc9e57cdb886073127d991f65d0f7 }
    - { path: src/components/common/headers/components/credits-left/index.module.scss, sha256: 9a46e44a4f599e280563ddbc3bd1dbcb178c9b74f7ca66d7a4eaef938900c846 }
    - { path: public/assets/images/header/icon_credit.svg, sha256: fe13827402ad85122458ba7de848cbf47e0cb4401d319a6ebc8bfd511245b1d1 }
    - { path: public/assets/images/header/icon_add.svg, sha256: 2bb3284338e2893ec78d567ba65f511f11ca1b004d0948e6b737fe3417758457 }
    - { path: public/assets/images/loading.png, sha256: 4d7e85e273326aca344a9e3db9766661ea15398b626694508d1d6ca03d4b0ef0 }
    - { path: src/components/result-page/video-object-remover/index.module.scss, sha256: d5ba8af85582f520547c6863e878a115dedb39000673edfea13f83ce5184edb6 }
figma: { status: pending, fileKey: null, componentSetNodeId: null, variants: [] }
implementation:
  importPath: platform/ui/credit-controls/index.js
  storyId: ui-credit-controls
  framework: react
  dependencies: [ react, platform-ui-button ]
  removedDependencies: [ redux, authentication, credit-service, pricing-modal, local-storage, i18n, lodash, analytics ]
  localFiles:
    - path: platform/ui/credit-controls/CreditControls.jsx
      sha256: 5aa681a8fa3d6a880af5f108ccf31fde2b5a81ecbc48da19cd814feddbdf3944
    - path: platform/ui/credit-controls/CreditControls.module.scss
      sha256: 2fcc0acb1864610275b6d0e1826e51baceca7158a13d2ebcaf1aa10542da7ad8
    - path: platform/ui/credit-controls/CreditControls.stories.jsx
      sha256: 050d42c8164d821b9469c5e1a278d21cc11847de28943c4769d5846e26bd6213
    - path: platform/ui/credit-controls/index.js
      sha256: 5bcc58063e29497a2c720fb7c7c916230633f961b3645218193e31401d59e29b
  localFilesUpdated: 2026-09-15
publicApi:
  props:
    - { name: balance, required: false }
    - { name: cost, required: false }
    - { name: label, required: false }
    - { name: subtitle, required: false }
    - { name: disabled, required: false }
    - { name: isLoading, required: false }
    - { name: onClick, required: false }
  states: [ header-balance, compact-badge, generate-enabled, generate-disabled, loading, inert, generate-two-line, generate-two-line-with-credits ]
tokens:
  lockPath: platform/tokens/tokens.lock.json
  uses: [ --background-base, --fill-strong, --fill-weaker, --stroke-focus, --text-strong ]
assets:
  - { path: assets/icon/yco-credit-controls, usage: runtime }
review:
  pilotApprover: { actor: pm, id: collab-space-owner, date: 2026-09-02 }
  canonicalReview: { designer: approved, rd: pending }
decisionBasis:
  - The header balance and Generate cost share the RD production credit asset but accept synthetic values through pure props.
  - The PO approved balance 436 and Generate cost 10 without credit-service or purchase behavior.
  - Generate preserves the RD single-tool brand, disabled, loading and credit-pill states rather than inheriting the generic neutral button treatment.
  - "Loading-spinner de-duplication (2026-09-06): `GenerateActionBar` used to thread its own static `design-library/assets/icon/yco-credit-controls/loading.png` through as Button `children` for `isLoading`. Once `platform/ui/button/Button.jsx` grew its own built-in loading spinner (see button's own component.yaml), this component showed two spinners at once. Removed the `loading.png` usage and its now-dead `.loadingIcon` CSS rule; Button's own spinner is used instead. The asset itself is left in `design-library/assets/` unused for now rather than deleted, in case something else still needs it."
  - "Height per Figma's Footer Button handoff spec (2026-09): Text Only=42px (Button's own Medium, unmodified), Text with credits=48px, Two-line text=56px — the credit badge alone adds 6px over plain Medium, and having a subtitle line takes it to 56px regardless of whether a badge is also present. Implemented 2026-09-06: `.generateButton` overrides Button's Medium min-height to 48px (hardcoded per Designer instruction, not a new RD token — same rationale as button's own component.yaml height entry); `.generateButtonTwoLine` (applied whenever `subtitle` is set) overrides it further to 56px."
  - "Two-line text + credit badge (2026-09-06): added a `subtitle` prop to `GenerateActionBar` for the Figma \"Two-line text\" composition (e.g. \"Generate / 3 seconds for 12 Credits\", or with a credit badge alongside, e.g. \"Enhance Video / Full HD without watermark\"+badge) — both are 56px per the handoff spec regardless of the badge. Story names are deliberately generic (`GenerateTwoLine`, `GenerateTwoLineWithCredits`), not tied to any one feature's copy (e.g. not \"EnhanceVideo...\"), since other Generate-style actions will reuse this composition with their own label/subtitle text."
  - "Width-responsive side padding (2026-09-06): the same Figma handoff spec gives the action bar 24px side padding at \"W400 & mobile\" and 16px at \"W300\" — this reacts to how wide the panel this component is embedded in actually renders, not the browser viewport, so it's implemented as a CSS container query (`container-type: inline-size` on `.actionBar`) rather than a media query. An element cannot query its own containment context (verified empirically — the padding rule silently did nothing when both lived on `.actionBar` itself), so the query container is `.actionBar` and the padding lives one level in on a new `.actionBarInner` wrapper. 350px (the midpoint between the spec's named 300/400) is used as the breakpoint since the spec didn't give an exact cutoff — flag with Designer/RD if the real threshold differs. Verified in-browser: 24px padding at 400px+ container width, 16px at 350px and below."
  - "Current local hashes after the 2026-09-06 changes above: `CreditControls.jsx` → 73e59eb7e99fd635295874e98806631ab43f40ecde6605faa9a59e7f1ddab049; `CreditControls.module.scss` → bdb56f69c0748e6f3cfe7bda51d8737924cd3defcd26440315ecde3281aad854. Same deliberate-divergence status as button's own component.yaml — pending Designer/RD canonical review, not silently claimed as already approved."
  - "Designer canonical review approved 2026-09-06: current implementation reviewed and signed off. RD canonical review still pending."
  - "2026-09-10: Replaced undefined --font-weight-regular with the existing RD --font-weight-weak (400), matching the previous normal fallback."
  - "`.actionBar`'s own background+box-shadow removed (2026-09-15, requested live — \"接著settings就不用在包在一個container裡面了，離最後一個container 16px就好\", in the context of ToolPageLayout's own footer becoming conditionally sticky rather than a permanently-fixed bottom bar — see that component's own decisionBasis): those two properties were built for a bar that's always visually separated from scrolled content behind it; now that GenerateActionBar commonly renders in plain document flow right after a panel's settings, they read as an unwanted boxed card around just the button. ToolPageLayout's own `.footer` wrapper already carries the background needed for the one case (a tall settings panel, actually stuck) where opacity still matters, so nothing was lost."
  - "`.actionBarInner` wrapper div and its own width-responsive 16px/24px padding removed entirely (2026-09-15, reported live with a DevTools box-model inspect — \"不需要有綠色這層padding\"), along with `.actionBar`'s `container-type: inline-size` (its only reason for existing was that padding's `@container` query): both existed to inset this bar's content when it spanned edge-to-edge with no outer margin of its own — the same full-bleed treatment the entry above already retired. With no outer margin left to compensate for, the inset just made the button narrower than the settings content above it instead of matching its width."
  - "Credit badge now hidden whenever `disabled`, not just while `isLoading`, and the button drops to the spec's own 42px \"Text Only\" height (previously never implemented — only 48px/with-credit and 56px/two-line existed) in that case (2026-09-15, requested live — \"當disable的時候無法偵測所需credits所以可以把credit拿掉，然後按鈕高度設回42px\"): while disabled, the real cost depends on a selection that isn't finalized yet, so showing a specific number was misleading. New `showCredit = !isLoading && !disabled` gates both the badge and which height class applies; the two-line case still wins over both regardless of credit, matching the height spec's own stated precedence."
  - "`assets/icon/yco-credit-controls/credit.svg` replaced with a new asset supplied directly by the designer (2026-09-15, \"credits可以幫我取代成這顆看看嗎，你現在的看起來都有點透明透明的\" — the old asset was a ~150-path radial-gradient illustration that read as washed out at this icon's actual render size; the replacement is a simple gradient-filled circle + glyph, the same shape category but far higher-contrast at 18-24px). Same filename/path, so both CreditBadge and CreditControl (its only two consumers) picked it up with no import changes. New sha256: 4103b57ceacfc0a3c67755b49ecb313ba0fa0a540b97845684360e259572fe95."
  - "`.badge` height corrected 32px (a min-height:24px that never actually won against the small/bold text's own 24px line-height + 4px top/bottom padding) → an explicit 28px (2026-09-15, reported live with an inspect of the Generate button's own credit badge — \"這顆的高度不是32px是28px\"). No token exists at 28px (the scale jumps 24→32), so a literal px, matching this codebase's other Designer-specified in-between sizes (Ratio's own 18px icon, 2px corner radius)."
  - "\"Text with credits\" button height revised 48px → 44px — a deliberate spec change, not a fix (2026-09-15, requested live — \"有credits的時候本來和你說高度是48px我們改成44px\"). \"Text Only\" (42px) and \"Two-line text\" (56px) are unchanged; only this one composition's height moved."
