# Authenticated home layout rules

Use the RD `/home` composition: a fixed global header, a desktop category rail and a
wide content region made of quick categories, recent tools and categorised tool cards.
The Gallery and pricing entries remain available in the header at every viewport.

## Removed production dependencies

CMS content, Redux account state, authentication redirects, analytics, file upload and
cross-page state recovery are not part of this Surface preview.
