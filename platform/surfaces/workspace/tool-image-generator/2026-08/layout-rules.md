# Image Generator Tool layout rules

Treat prompt or source intake and result selection as the two primary regions. The
feature may use a side-by-side or stacked arrangement, but the selected result and
generation action must remain easy to locate at every configured viewport.

## Decision basis

Generation settings vary by feature, while the intake-to-processing-to-results model is
stable enough to reuse.

