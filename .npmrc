# react-textfit@1.1.1 and react-dotdotdot@1.3.1 declare React 15/16 peers but the
# RD-port surfaces run them on React 18. Without this, `npm ci` (Vercel's install
# command) fails with ERESOLVE before the build ever starts.
legacy-peer-deps=true
